# 局部搜索效率指标说明

## 指标含义与计算方式

### 1. 总搜索次数 (Total Searches)
**含义**：该方法在所有问题上执行局部搜索的总次数。

**计算方式**：统计所有日志文件中匹配 `Cluster rep idx=... : F: ... -> ..., FEs: ...` 格式的行数。

**示例**：Adam 方法共执行了 1449 次局部搜索。

---

### 2. 改进次数 (Improved Count) 和改进率 (Improved Ratio)
**含义**：
- **改进次数**：局部搜索后适应度值降低（改进）的次数
- **改进率**：改进次数占总搜索次数的百分比

**计算方式**：
```
改进次数 = 统计日志中标记为 "IMPROVED" 的次数
改进率 = 改进次数 / 总搜索次数 × 100%
```

**示例**：Adam 方法中，1345 次搜索实现了改进，改进率为 92.8%，说明该方法在大多数情况下都能找到更好的解。

---

### 3. FEs=0 的次数和比例
**含义**：
- **FEs=0 的次数**：局部搜索过程中没有消耗函数评估次数（FEs=0）的次数
- **FEs=0 的比例**：FEs=0 的次数占总搜索次数的百分比

**计算方式**：
```
FEs=0 的次数 = 统计日志中 FEs: 0 的次数
FEs=0 的比例 = FEs=0 的次数 / 总搜索次数 × 100%
```

**为什么会出现 FEs=0？**
- 对于 RBF 和 GP 等代理模型方法，如果历史数据不足或代理模型构建失败，会直接返回原值，不进行真实函数评估
- 这种情况下 FEs=0，但可能仍然有改进（因为使用了历史缓存）

**示例**：Adam 方法中，59 次搜索的 FEs=0（4.1%），说明大部分搜索都进行了真实函数评估。

---

### 4. 平均 FEs (Average FEs)
**含义**：每次局部搜索平均消耗的函数评估次数。

**计算方式**：
```
平均 FEs = Σ(FEs_i) / 总搜索次数
```

**示例**：Adam 方法的平均 FEs 为 104.56，说明每次局部搜索平均需要约 105 次函数评估。

---

### 5. 平均 F 下降 (Average F Reduction) 和标准差
**含义**：
- **F 下降**：单次局部搜索中适应度值的减少量（改进量）
- **平均 F 下降**：所有搜索的平均改进量
- **标准差**：F 下降值的离散程度

**计算方式**：
```
对于每次搜索：
  F下降 = f_old - f_new  （如果 f_new < f_old，即改进）
  F下降 = 0              （如果 f_new ≥ f_old，即未改进）

平均 F 下降 = Σ(F下降_i) / 总搜索次数
标准差 = √(Σ(F下降_i - 平均)^2 / (n-1))
```

**注意**：
- 只有改进的搜索才计入 F 下降（未改进的为 0）
- 标准差很大（如 5.285822e+04）说明不同搜索的改进量差异很大

**示例**：Adam 方法的平均 F 下降为 8.523815e+03，标准差为 5.285822e+04，说明：
- 平均每次搜索能改进约 8524 个适应度单位
- 但不同搜索的改进量差异很大（标准差是均值的 6 倍多）

---

### 6. 平均效率 (Average Efficiency) 和标准差
**含义**：
- **效率**：单位函数评估次数带来的适应度改进量
- **平均效率**：所有搜索的平均效率
- **标准差**：效率值的离散程度

**计算方式**：
```
对于每次搜索：
  效率 = F下降 / max(FEs, 1)
  
  注意：如果 FEs=0，则使用 max(FEs, 1) = 1 作为分母
       这样可以避免除零，同时保持效率的合理性

平均效率 = Σ(效率_i) / 总搜索次数
标准差 = √(Σ(效率_i - 平均)^2 / (n-1))
```

**物理意义**：
- 效率越高，说明该方法用更少的函数评估次数获得更大的改进
- 效率的单位是：适应度单位 / 函数评估次数

**示例**：Adam 方法的平均效率为 7.820014e+01，说明：
- 平均每次函数评估能带来约 78.2 个适应度单位的改进
- 但标准差很大（4.849378e+02），说明不同搜索的效率差异很大

---

### 7. 中位数效率 (Median Efficiency)
**含义**：所有搜索效率值的中位数。

**计算方式**：
```
将所有效率值排序，取中间值（或中间两个值的平均）
```

**为什么使用中位数？**
- 中位数对异常值不敏感
- 当数据分布偏斜（标准差很大）时，中位数比平均值更能代表"典型"效率
- 可以过滤掉极端值的影响

**示例**：Adam 方法的中位数效率为 2.031963e-03，远小于平均值（7.820014e+01），说明：
- 大部分搜索的效率较低（约 0.002）
- 少数搜索的效率极高，拉高了平均值
- 中位数更能反映"典型"搜索的效率水平

---

### 8. 各问题详细统计
**含义**：针对每个测试问题（F2, F3, F4, Rastrigin, Griewank, Ackley）的单独统计。

**包含指标**：
- **搜索次数**：在该问题上执行的局部搜索次数
- **平均效率**：该问题上的平均效率
- **中位数效率**：该问题上的中位数效率
- **平均 F 下降**：该问题上的平均 F 下降

**用途**：
- 比较不同方法在不同问题上的表现
- 识别方法在特定问题上的优势或劣势
- 分析问题特性对方法效率的影响

**示例**：Adam 方法在 F2 问题上：
- 执行了 306 次搜索
- 平均效率为 4.615135e-03（相对较低）
- 中位数效率为 1.296917e-03（更接近典型值）
- 平均 F 下降为 5.030497e-01（约 0.5 个适应度单位）

---

## 指标解读建议

### 1. 效率指标的选择
- **使用平均值**：当数据分布相对均匀时
- **使用中位数**：当数据分布偏斜、存在极端值时（如本例）

### 2. 标准差的意义
- **标准差大**：说明方法表现不稳定，有些搜索很有效，有些无效
- **标准差小**：说明方法表现稳定，每次搜索的效果相对一致

### 3. FEs=0 的情况
- **比例高**：说明该方法经常回退到原值（可能是历史数据不足或代理模型失败）
- **比例低**：说明该方法大多数情况下都能进行有效的局部搜索

### 4. 改进率
- **高改进率**：说明该方法在大多数情况下都能找到更好的解
- **低改进率**：说明该方法可能不适合当前问题，或者搜索策略需要调整

---

## 实际应用

这些指标可以帮助：
1. **方法比较**：比较不同局部搜索方法的效率
2. **问题分析**：识别哪些问题对方法更具挑战性
3. **参数调优**：根据效率指标调整算法参数
4. **资源规划**：根据平均 FEs 估算计算资源需求

